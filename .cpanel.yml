---
deployment:
  tasks:
    - export DEPLOY_PATH=/home/femseapto/public_html/development/
    - rsync -avz --exclude=.git back/ $DEPLOY_PATH/
    - cd front/
    - ng build --configuration production --outputPath=$DEPLOY_PATH